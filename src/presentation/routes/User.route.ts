import express from 'express';
// import { authenticate, authorize } from '../middlewares/Auth.middleware';

const router = express.Router();


// router.get('/settings', authenticate, authorize(['user', 'farmer', 'admin']))

export default router;